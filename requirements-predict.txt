# predict.py's actual runtime dependencies -- deliberately NOT scikit-learn
# (see report.md: importing sklearn alone costs ~2s, vs ~60ms for everything
# predict.py actually does). Use requirements.txt for training/dev instead.
numpy==2.5.1
opencv-python-headless==5.0.0.93
