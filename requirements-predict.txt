# predict.py's actual runtime dependencies -- deliberately NOT scikit-learn
# or torch (see EXPERIMENTS.md / report.md: onnxruntime is the lightweight
# runtime for the frozen CNN backbone; torch would conflict with numpy>=2
# and costs far more to import). Use requirements.txt for training/dev.
numpy==2.5.1
pillow==12.3.0
onnxruntime==1.23.2
